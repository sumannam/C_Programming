// ex04.c
#include <stdio.h>
#include <ctype.h>

int main(void)
{
	char str1[10];
	int length;
	scanf(“%s”, str1);
	for(length = 0; s[length] != NULL; length++);
		printf(“The length of string: %d\n”, length);
		
	return 0;
}
